export const contractsList = [
  {
    id: "contract-1",
    name: "VulnerableBank",
    category: "bank" as const,
    variant: "vulnerable" as const,
    language: "solidity" as const,
    version: "^0.8.20",
    source: `// Vulnerable to reentrancy\ncontract VulnerableBank {\n    mapping(address => uint) public balances;\n\n    function deposit() external payable {\n        balances[msg.sender] += msg.value;\n    }\n\n    function withdraw(uint amount) external {\n        require(balances[msg.sender] >= amount, "Insuf. balance");\n        (bool ok,) = msg.sender.call{value: amount}("");\n        if (ok) balances[msg.sender] -= amount;\n    }\n}`,
    highlightLines: [8, 9, 10, 11],
    description: "Bank contract vulnerable to reentrancy attack. The balance is updated after the external call.",
    vulnerability: "Reentrancy",
    severity: "critical" as const,
    exploitSteps: ["Deploy attacker contract", "Call withdraw() which triggers fallback()", "Fallback re-enters withdraw() before balance update", "Drain entire contract balance"],
    mitigation: "Use Checks-Effects-Interactions pattern. Update balance before external call.",
    testFile: "ReentrancyTest.t.sol",
  },
  {
    id: "contract-2",
    name: "SecureBank",
    category: "bank" as const,
    variant: "secure" as const,
    language: "solidity" as const,
    version: "^0.8.20",
    source: `// Secure: Checks-Effects-Interactions\ncontract SecureBank {\n    mapping(address => uint) public balances;\n    mapping(address => bool) public withdrawing;\n    modifier noReentrancy() {\n        require(!withdrawing[msg.sender], "No reentrancy");\n        withdrawing[msg.sender] = true;\n        _;\n        withdrawing[msg.sender] = false;\n    }\n\n    function deposit() external payable {\n        balances[msg.sender] += msg.value;\n    }\n\n    function withdraw(uint amount) external noReentrancy {\n        require(balances[msg.sender] >= amount, "Insuf. balance");\n        balances[msg.sender] -= amount;\n        (bool ok,) = msg.sender.call{value: amount}("");\n        require(ok, "Transfer failed");\n    }\n}`,
    highlightLines: [16, 17, 18],
    description: "Bank contract secured with reentrancy guard and CEI pattern.",
    vulnerability: null,
    severity: null,
    exploitSteps: [],
    mitigation: "Reentrancy guard prevents recursive calls. Balance updated before external transfer.",
    testFile: "ReentrancyTest.t.sol",
  },
  {
    id: "contract-3",
    name: "VulnerableToken",
    category: "token" as const,
    variant: "vulnerable" as const,
    language: "solidity" as const,
    version: "^0.8.20",
    source: `// Vulnerable: no access control on mint\ncontract VulnerableToken {\n    mapping(address => uint) public balances;\n    address public owner;\n\n    function mint(address to, uint amount) external {\n        balances[to] += amount;\n    }\n\n    function transfer(address to, uint amount) external {\n        require(balances[msg.sender] >= amount);\n        balances[msg.sender] -= amount;\n        balances[to] += amount;\n    }\n}`,
    highlightLines: [4, 5],
    description: "Token with no access control on mint function. Anyone can mint.",
    vulnerability: "Access Control",
    severity: "critical" as const,
    exploitSteps: ["Call mint() with any address and amount", "Create unlimited tokens", "Dump tokens on market"],
    mitigation: "Add onlyOwner modifier to mint function.",
    testFile: "AccessControlTest.t.sol",
  },
  {
    id: "contract-4",
    name: "SecureToken",
    category: "token" as const,
    variant: "secure" as const,
    language: "solidity" as const,
    version: "^0.8.20",
    source: `// Secure: access control on mint\ncontract SecureToken {\n    mapping(address => uint) public balances;\n    address public owner;\n\n    modifier onlyOwner() {\n        require(msg.sender == owner, "Not owner");\n        _;\n    }\n\n    function mint(address to, uint amount) external onlyOwner {\n        balances[to] += amount;\n    }\n\n    function transfer(address to, uint amount) external {\n        require(balances[msg.sender] >= amount);\n        balances[msg.sender] -= amount;\n        balances[to] += amount;\n    }\n}`,
    highlightLines: [6, 7, 8, 9, 11],
    description: "Token with proper access control on mint.",
    vulnerability: null,
    severity: null,
    exploitSteps: [],
    mitigation: "onlyOwner modifier restricts mint to contract owner.",
    testFile: "AccessControlTest.t.sol",
  },
  {
    id: "contract-5",
    name: "VulnerableRandomness",
    category: "randomness" as const,
    variant: "vulnerable" as const,
    language: "solidity" as const,
    version: "^0.8.20",
    source: `// Vulnerable: bad randomness\ncontract VulnerableRandomness {\n    function random() internal view returns (uint) {\n        return uint(keccak256(abi.encodePacked(\n            block.timestamp,\n            block.prevrandao,\n            msg.sender\n        )));\n    }\n\n    function lottery() external {\n        uint winner = random() % 100;\n        // distribute prize based on winner\n    }\n}`,
    highlightLines: [3, 4, 5, 6, 7, 8, 9],
    description: "Bad randomness using block.timestamp and prevrandao which can be manipulated by validators.",
    vulnerability: "Bad Randomness",
    severity: "high" as const,
    exploitSteps: ["Validator manipulates block.timestamp", "Predict random output", "Win lottery unfairly"],
    mitigation: "Use Chainlink VRF or commit-reveal scheme.",
    testFile: "BadRandomnessTest.t.sol",
  },
  {
    id: "contract-6",
    name: "VulnerableDAO",
    category: "dao" as const,
    variant: "vulnerable" as const,
    language: "solidity" as const,
    version: "^0.8.20",
    source: `// Vulnerable: no snapshot, low quorum\ncontract VulnerableDAO {\n    mapping(address => uint) public votes;\n    uint public quorum = 1;\n\n    function createProposal(bytes calldata data) external {\n        // no proposal threshold\n    }\n\n    function execute(address target, bytes calldata data) external {\n        require(totalVotes >= quorum, "Low quorum");\n        (bool ok,) = target.call(data);\n        require(ok);\n    }\n}`,
    highlightLines: [3, 7, 9, 10],
    description: "DAO with low quorum and no snapshot mechanism. Flash loan can be used to pass proposals.",
    vulnerability: "Governance Attack",
    severity: "critical" as const,
    exploitSteps: ["Flash loan large amount of governance tokens", "Vote on malicious proposal", "Execute treasury drain", "Repay flash loan"],
    mitigation: "Use snapshot voting, require quorum and proposal threshold, add timelock.",
    testFile: "GovernanceTest.t.sol",
  },
  {
    id: "contract-7",
    name: "VulnerableUpgradeable",
    category: "upgradeable" as const,
    variant: "vulnerable" as const,
    language: "solidity" as const,
    version: "^0.8.20",
    source: `// Vulnerable: no access control on upgrade\ncontract VulnerableUpgradeable {\n    address public implementation;\n\n    function upgradeTo(address newImpl) external {\n        implementation = newImpl;\n    }\n\n    fallback() external payable {\n        address impl = implementation;\n        require(impl != address(0));\n        assembly {\n            calldatacopy(0, 0, calldatasize())\n            let result := delegatecall(gas(), impl, 0, calldatasize(), 0, 0)\n            returndatacopy(0, 0, returndatasize())\n            switch result\n            case 0 { revert(0, returndatasize()) }\n            default { return(0, returndatasize()) }\n        }\n    }\n}`,
    highlightLines: [3, 4, 5],
    description: "Upgradeable contract with no access control on upgradeTo function.",
    vulnerability: "Upgradeability Risk",
    severity: "critical" as const,
    exploitSteps: ["Call upgradeTo() with malicious implementation", "delegatecall executes attacker's code", "Drain contract funds"],
    mitigation: "Add onlyOwner modifier or use a proxy with proper access control.",
    testFile: "UpgradeabilityTest.t.sol",
  },
];
